// arrays

const arr = [1, 2, 3];

arr.length === 3

console.log(arr[0]) === 1;

//arr.push(x) adds item to end of array
//arr.pop() removes item from end of array
//arr.unshift(x) adds item to front of array
//arr.shift() removes item from front of array
//arr.reverse() flips array

const turtle = {
	name: 'marvin',
	age: 102,
	hungry: true
};

const bird = {};

const alabama = {
	region: 'south',
	capital: 'montgomery',
	governor: 'Kay Ivey',
	population: 4863300,
	proFootballTeam: false
}

const minnesota = {};